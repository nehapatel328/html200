function squareRoot() {
let number = prompt('Enter a number: ') //alert to screen

function squareRoot(numberToSquare) { //callback function with a name of 'square'
  return Math.pow(numberToSquare,2); //returns square of a number with a variable of 'numberToSquare' or what the user inputs
}

alert(squareRoot(number)); //print alert to the screen with the user input squared

//new version keeps functions separate
}
